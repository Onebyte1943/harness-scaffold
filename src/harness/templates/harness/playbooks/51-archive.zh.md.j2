---
name: hx-archive
phase: review
flow: [quick, standard, full, epic]
---
# /hx-archive

## Purpose

变更合入 main 之后,刷新 **项目知识库** 让它反映新代码,并执行
**引用校验器** 确保没有悬空的 `path:line` 引用。没有独立的 merge
command —— git / GitLab MR 负责合并。也没有 delta 树需要回流。

## Prerequisites

- 变更已合入 main 并在目标环境完成部署 / 验证。
- `/hx-review` 的 verdict 是 `ship`。
- 所有 review must-fix 项已处理。

## Inputs

- 已合入的变更:`specs/<NNN>-<slug>/`。
- 代码库的新状态 (现在它是事实来源)。
- `.harness/knowledge/` (当前版本;即将被刷新)。

## Steps

1. **确认变更已上线。** 不要为未发布的工作做 archive。
2. **刷新受影响的 knowledge 文档。** 对 7 份 knowledge 文件中受本次
   变更影响的小节做更新,使其描述合入后的代码 (带指向新位置的
   `path:line` 引用):
   - `architecture.md` —— 模块 / 边界 / 数据流变化。
   - `tech-stack.md` —— 新依赖、新模式、被弃用的用法。
   - `business.md` —— 仅当业务不变量真正发生变化时 (且需与非代码
     来源交叉验证)。
   - `conventions.md` —— 引入了新的标杆示例时。
   - `glossary.md` —— 域词被新增或重命名时。
   - `setup-and-verify.md` —— 命令或 verify 路径发生变化时。
   - `how-to/*` —— 受变更影响的 recipe 需刷新或新增。
   - `product.md` —— 仅当能力被新增 / 移除时。
3. **运行引用校验器。** 刷新后的文档以及 `specs/<NNN>-<slug>/` 中
   每一条 `path:line` 与符号引用都必须可解析。失败则阻塞 archive。
4. **保留 `specs/<NNN>-<slug>/` 不动。** 不做目录搬移。git history
   就是 audit trail。
5. **在 `.agent/progress.md` 追加一行**,标记该变更已收尾。

## Outputs

- 更新后的 `.harness/knowledge/` 文件。
- `.agent/progress.md` 中的收尾条目。
- 引用校验器干净通过。

## Exit Criteria

- 知识库反映了合入后的代码。
- 没有悬空的 `path:line` 引用。
- `specs/<NNN>-<slug>/` 作为该变更的永久记录被保留。

## Gate

新的变更周期 MAY 在 archive 完成之前开始,但团队 SHOULD NOT 让
未 archive 的变更堆积 —— knowledge 失新会复利式恶化。

---

## Appendix A — archive checklist (T11)

> Provenance
> - Authoritative paradigm: spec-kit "specs ship with the code" + citation fidelity — no delta tree, no separate merge command (GitLab MR handles merging); `specs/<NNN>-<slug>/` stays in place as the permanent record
> - Investigation protocol: only run on merged work; diff the merged code against the 7 knowledge docs and refresh affected sections with new `path:line` anchors; run the reference validator so no dangling citation survives in knowledge or active specs
> - Output form: post-merge checklist (deployed · knowledge refreshed · validator clean · progress.md closed · specs left in place) + a closing row appended to `.agent/progress.md`

```markdown
# Archive — ⟨id⟩  (run after merge to main via GitLab MR)

- [ ] Change deployed / verified
- [ ] Refresh affected `.harness/knowledge/` docs to reflect merged code
- [ ] Run reference validator (no dangling path:line)
- [ ] Append closed entry to `.agent/progress.md`
- [ ] Leave `specs/⟨NNN⟩-⟨slug⟩/` in place (git history = audit)
```
