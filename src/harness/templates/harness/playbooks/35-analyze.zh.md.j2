---
name: hx-analyze
phase: change
flow: [full, epic]
---
# /hx-analyze

## Purpose

跨产物一致性检查。在任何代码动笔之前,确认 constitution、knowledge、
proposal、clarify、design、plan、tasks 这些产物彼此对得上。

## Prerequisites

- `specs/<NNN>-<slug>/proposal.md`、`design.md`、`plan.md`、`tasks.md`
  全部存在。
- `.harness/memory/constitution.md` 已加载。

## Inputs

- `.harness/memory/constitution.md` —— `[enforceable]` 条款是硬闸口。
- `.harness/knowledge/` —— 用作 ground-truth 校验。
- 所有 `specs/<NNN>-<slug>/*.md`。

## Steps

1. **REQ 覆盖**:`design.md` 中每个 REQ-NNN 都被 `tasks.md` 中至少
   一个任务 (以及一个测试任务) 引用。
2. **DEC 覆盖**:每个 DEC-NNN 都指向 `.harness/knowledge/adr/` 下
   一个真实存在的 ADR 文件。
3. **Constitution 检查**:把每条 `[enforceable]` 章程条款与 plan 和
   tasks 交叉对照。任何违反 MUST 通过重新 plan 解决,或通过 proposal
   + ratify 显式豁免。
4. **Scope 蔓延**:任何无法追溯回 proposal/design 中某个 REQ 的任务
   都是 scope creep。Flag 出来。
5. **Knowledge 对齐**:design 中对当前架构的论断 MUST 与
   `.harness/knowledge/architecture.md` 一致。若 knowledge 错了,
   开一个 follow-up,稍后通过 `/hx-archive` 更新。
6. **冲突扫描**:检测任何两个产物在 behavior、interface 或 constraint
   上的不一致。
7. **撰写 `specs/<NNN>-<slug>/analyze.md`** —— 按三档分类:
   - **CONFLICT** (在 implement 前 MUST 解决)。
   - **GAP** (在 implement 前 MUST 补齐)。
   - **WARNING** (审查;带说明可以继续)。

## Outputs

- `specs/<NNN>-<slug>/analyze.md` —— 一致性报告。

## Exit Criteria

- 0 个未解决的 CONFLICT。
- 0 个未解决的 GAP。
- 完整 traceability matrix:constitution ↔ knowledge ↔ proposal ↔
  design ↔ tasks。

## Gate

只要还有任何未解决的 CONFLICT 或 GAP,`/hx-implement` MUST NOT 运行。

---

## Appendix A — analyze report block

> Provenance
> - Authoritative paradigm: spec-kit analyze + constitution `[enforceable]` cross-check
> - Investigation protocol: read `.harness/memory/constitution.md` + `.harness/knowledge/` + every `specs/<NNN>-<slug>/*.md`; check REQ coverage, DEC ↔ ADR coverage, constitution clause adherence, scope creep, knowledge alignment, inter-artifact conflicts
> - Output form: three-tier report — **CONFLICT** (must resolve) · **GAP** (must fill) · **WARNING** (may proceed with rationale) — plus a full traceability matrix `REQ ↔ Design ↔ Plan ↔ Task ↔ Test ↔ ADR ↔ Constitution`

```markdown
# Analyze — ⟨id⟩

## CONFLICT (must resolve before implement)
- ⟨artifact A ↔ artifact B⟩ — ⟨what disagrees⟩ — owner ⟨…⟩

## GAP (must fill before implement)
- REQ-NNN / DEC-NNN missing in ⟨tasks / ADR⟩ — owner ⟨…⟩

## WARNING (review, may proceed with rationale)
- ⟨…⟩

## Traceability matrix
| REQ | Design | Plan | Task | Test | ADR | Constitution |
| --- | ------ | ---- | ---- | ---- | --- | ------------ |
```
