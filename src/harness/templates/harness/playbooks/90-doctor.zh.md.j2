---
name: hx-doctor
phase: steer
flow: [quick, standard, full, epic]
---
# /hx-doctor

## Purpose

对 harness 自身做体检。在漂移 / 死规则 / 预算超支 / eval 回归 /
内部矛盾被复利放大之前把它们抓出来。区别于 `/hx-analyze` (检查
planning artifact) 和 `/hx-review` (检查实现 vs 计划) —— `/hx-doctor`
检查的是 harness 这套机制本身的自洽性。

## Prerequisites

- Harness 已初始化 (`.harness/` 存在)。

## Inputs

- `.harness/` 下的全部内容。
- `AGENTS.md`、`CLAUDE.md`、`.codex/commands/` (如存在)。
- `specs/` 近期变更 (用于 design-doc 抽样评分)。
- `.harness/evals/` 回归样本。
- 可选的使用 telemetry (哪些 playbook 被触发、哪些规则被引用)。

## Steps

### Sync & Consistency

1. **规则 ↔ guardrail 同步。** 每条 constitution 原则都应至少被一个
   playbook step / lint rule / arch-test / review check 引用。孤儿
   规则 → 建议裁掉。没有底层规则支撑的 guardrail → 建议裁掉。
2. **指南互不矛盾。** 跨 AGENTS.md / constitution / playbooks /
   knowledge 扫一遍,找出冲突指令。

### Budgets

3. **文件体量预算。**
   - `AGENTS.md` < 32 KiB。
   - `.harness/memory/constitution.md` ≤ 一屏原则。
   - knowledge 文档不超量 (cohesion 优先;单文档膨胀时报警)。

### Design Quality (sampled)

4. **Design-doc 评分** (§12.4 A)。抽样近期 `specs/*/design.md`,
   对各维度打分:groundedness / unambiguity / completeness /
   self-containment / right-sized / modularity / traceability /
   consistency / blockers cleared。

### Hygiene

5. **死规则 / 死 guardrail / 死 playbook step。** 找出在任何 traced
   run 里都没被引用过的项。建议裁掉。
6. **漂移指标。** 把 `.harness/knowledge/architecture.md` 与真实模块
   图对照。把 constitution 的 `[enforceable]` 条款与 lint / arch-test
   的覆盖对照。

### Validation

7. **引用校验器** —— knowledge 与活跃 specs 中每条 `path:line` /
   符号引用都可解析。
8. **Knowledge ↔ code 漂移** —— `architecture.md`、`tech-stack.md`、
   `conventions.md` 中点名的锚点在代码库里仍然存在。
9. **遗留布局告警** —— 如果出现 `harness-artifacts/` 或根目录的
   `memory/`,提示一次性迁移警告 (这套布局在 design v3 已变更)。

### Eval Regression Gate

10. **跑一遍 `.harness/evals/`** 样本 (clean-context 运行)。和基线
    对比:first-try-pass 率、clarify 提问数、review must-fix 数、
    rework 量。任何在 `.harness/**` 或 `AGENTS.md` 变更上的回归 → block。

### Report

11. 按 *Appendix A* 给出分级 finding:
    - **CRITICAL** —— 引用断裂、预算超限、eval 回归。
    - **WARNING** —— 漂移指标、死规则、内部矛盾。
    - **INFO** —— 裁剪候选、design-score 差量。

## Outputs

- Doctor 报告:分级 finding + 具体补救建议。

## Exit Criteria

- 0 个断裂引用。
- 0 个预算超限。
- 0 个 harness 变更引发的 eval 回归。

## Gate

`/hx-doctor` 不会 block 其他 command。**但**任何 CRITICAL finding
都应在下一个变更周期开始前先解决。

---

## Appendix A — doctor report template (T12)

> Provenance
> - Authoritative paradigm: Böckeler "evaluate harness coverage and quality" — distinct from `/hx-analyze` (planning artifacts) and `/hx-review` (impl vs plan); this one checks the harness mechanism's self-consistency, drift, and eval regression
> - Investigation protocol: scan rule ↔ guardrail mapping (constitution principle ↔ playbook step / lint / arch-test / review check); validate every `path:line` and symbol citation across knowledge + active specs; diff `.harness/knowledge/architecture.md` anchors vs the live module graph; sample recent `specs/*/design.md` for §12.4 A scoring; replay `.harness/evals/` samples and compare to baseline (first-try-pass · clarify-questions · review must-fix · rework)
> - Output form: tri-tier self-check report — **CRITICAL** (broken refs · budget exceeded · eval regression) · **WARNING** (drift · dead rules · contradictions) · **INFO** (prune candidates · design-score deltas) — with concrete remediation per finding

```markdown
# Harness self-check — ⟨date⟩

## Rule ↔ guardrail sync
⟨rules without guard / guards without rule⟩

## Guide contradictions
⟨…⟩

## Budgets
- AGENTS.md ⟨KB⟩ / 32 KB
- constitution.md ⟨n principles⟩ / target ≤ 9
- oversized files ⟨…⟩

## Design doc score (sampled)
⟨per-dimension pass rate⟩

## Reference & drift
- Dangling references: ⟨n⟩
- Knowledge ↔ code drift: ⟨n⟩

## Eval regression
- first-try-pass: ⟨delta vs baseline⟩
- clarify-questions: ⟨delta⟩
- review must-fix: ⟨delta⟩
- rework: ⟨delta⟩

## Dead rules / guards / steps
⟨never-fired → prune candidates⟩

## Legacy layout
⟨harness-artifacts/ or root memory/ detected → migration note⟩

## Recommendations
⟨tighten / simplify / adjust thresholds⟩
```
