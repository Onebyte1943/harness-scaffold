---
name: hx-implement
phase: change
flow: [quick, standard, full, epic]
---
# /hx-implement

## Purpose

按 `specs/<NNN>-<slug>/tasks.md` 中的任务,逐个执行 —— 遵循 TDD 优先
顺序并完成端到端自验证。每个任务在进入下一个之前都是"完全可工作"
的状态 (red → green → refactor → verify)。如果在实现中发现上游
(spec / design / plan) 存在缺陷,回流到对应的 command,而不是
在实现层打补丁。

## Prerequisites

- `quick` flow:`proposal.md` (以及可选的 `clarify.md`) 存在。
- `standard`+:`tasks.md` 存在;对 `full`+, `analyze.md` 中没有
  未解决的 CONFLICT/GAP。
- `.harness/memory/constitution.md` 已加载。
- `.harness/scripts/{{ script_shell }}/verify.sh` 可用。

## Inputs

- `specs/<NNN>-<slug>/tasks.md` —— 有序任务拆分。
- `specs/<NNN>-<slug>/design.md` —— 接口、REQ-NNN ID、可追溯性。
- `specs/<NNN>-<slug>/plan.md` —— 编排顺序与决策。
- `AGENTS.md` —— 边界 (Always / Ask / Never)。
- `.harness/memory/constitution.md` —— `[enforceable]` 条款即闸口。
- `.agent/progress.md` —— 跨会话进度日志 (首次运行时创建)。

## Steps

1. **首次运行时引导进度日志。** 如果 `.agent/progress.md` 不存在,
   使用 *Appendix A — Progress log seed* 中的模板创建它 (含父目录)。
2. **定位。** 运行 `pwd`;阅读 `.agent/progress.md` 与近期 `git log`;
   打开 `tasks.md`,选取下一个未完成的任务。逐任务判断 `[P]` 并行
   标记 (仅当文件 / 模块互不重叠时才安全)。
3. **实现一个任务。** TDD 顺序:行为类任务先写失败的测试;再让它
   通过;再 refactor。遵守 `AGENTS.md` 与 constitution 的
   `[enforceable]` 条款。
4. **端到端验证。** 通过真实入口运行该 feature —— 而不是只跑单测。
   然后执行 `.harness/scripts/{{ script_shell }}/verify.sh`
   (完整模式,经由 `/hx-verify`)。只有 verify 真正变绿时才把任务标记为完成。
5. **提交。** 描述清晰的 commit message (what + why,关联到 REQ-NNN)。
   在 `.agent/progress.md` 追加一行,包含 date / command / flow /
   summary / gate。
6. **绝不通过删测或弱化测试让 verify 变绿。** 测试失败时修实现。
   唯一的例外是任务明确说明的"行为变更",此时也必须显式更新测试,
   而不是悄悄改掉。

### Rollback / circuit-breaker (§14.6)

7. **上游缺陷回滚。** 如果实现中发现 design / plan / proposal 是错的
   (接口不可实现、缺需求、自相矛盾),STOP 编码。回流到对应的 command:
   - 需求缺失或歧义 → `/hx-clarify`,
   - 接口 / 数据模型 / 决策错误 → `/hx-design`,
   - 编排顺序错误 / 被阻塞 → `/hx-plan`,
   - 任务清单不完整或顺序错乱 → `/hx-tasks`。
   把这次回滚作为一行记录在 `.agent/progress.md`,同时在受影响的 artifact
   中加注释。**绝不** 在实现里绕过坏掉的 spec。
8. **Verify 失败熔断。** 如果同一个任务上 `/hx-verify` 连续失败 ≥ 3 次,
   且错误特征没有任何进展,HALT。不要把预算烧在猜测式重试上。
   在 `.agent/progress.md` 写一段简短诊断 (what failed / what you tried /
   current hypothesis),并请求人工介入 —— 通过显式的 clarify pass,
   或在 playbook 输出里把 blocker 暴露出来。目标是避免"调 → verify →
   调 → verify"却没有 root cause 的循环。

## Outputs

- 提交到工作分支的代码变更。
- 更新后的 `.agent/progress.md` —— 每个完成的任务或回滚一行。

## Exit Criteria

- 当前任务已完整实现并完成端到端验证。
- `/hx-verify` 绿 (lint + typecheck + tests + honesty)。
- 存在一个 commit,带有描述清晰的 message。
- `.agent/progress.md` 反映了新状态。
- 没有任何既有测试被删除或弱化。

## Gate

任务 MUST NOT 在没有"干净工作树 + 绿色 `/hx-verify`"的前提下被标记为
完成。`/hx-review` MUST NOT 在 `tasks.md` 中所有任务"已完成或已显式
延后并记录原因"之前运行。

---

## Appendix A — Progress log seed (T9)

> Provenance
> - Authoritative paradigm: Anthropic cross-session progress + end-to-end self-verify + upstream-flaw rollback / verify-failure circuit-breaker (§14.6)
> - Investigation protocol: append a row per `/hx-*` invocation or rollback (date · command · flow · spec · summary · gate); never mutate prior rows — the log is the cross-session memory and audit trail
> - Output form: append-only timeline table with state-transition rows (task complete · rollback · circuit-breaker halt) — the file a clean-context agent reads first to orient

引导 `.agent/progress.md` 时按以下模板原样使用。

```markdown
# Progress Log — {{ project_name }}

> Cross-session record of completed phases, rollbacks, and state
> transitions. Append-only.

---

## How to Use

After completing any `/hx-*` command (or on a rollback), append a row
below with:

- **Date** — when the entry was made.
- **Command** — which `/hx-*` ran (or `rollback → /hx-clarify` etc.).
- **Flow** — `quick` / `standard` / `full` / `epic`.
- **Spec** — `specs/<NNN>-<slug>/` if applicable.
- **Summary** — one line: what happened.
- **Gate** — `pass` / `fail` / `n/a`.

---

## Log

| Date | Command | Flow | Spec | Summary | Gate |
| ---- | ------- | ---- | ---- | ------- | ---- |
```
